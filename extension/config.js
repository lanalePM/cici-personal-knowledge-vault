/**
 * Backend URL for the Cici web app (no trailing slash).
 *
 * Development: leave as http://localhost:3000
 * Production: set to your deployed URL (e.g. https://your-app.vercel.app)
 *
 * After changing this value, reload the extension in chrome://extensions
 */
const CICI_API_BASE = "http://localhost:3000";
